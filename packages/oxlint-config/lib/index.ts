import next from './next/recommended.json' with { type: 'json' };
import react from './react/recommended.json' with { type: 'json' };
import recommended from './recommended.json' with { type: 'json' };

export const configs = {
  recommended,
  react,
  next,
};

export default { configs };
